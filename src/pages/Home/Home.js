const Home = (props) => {
  return (
    <h1 className={props.className} style={{ backgroundColor: "red" }}>
      Home page, all recent post must be showed here
    </h1>
  );
};

export default Home;
